

#include <bits/stdc++.h>
using namespace std;

int main() {
  // there are data types like:
  // int, long, long long,
  // char, float, double.

  // to get a whole line as a input you have to use a internal function:
  string hello; // declare a string variable
  getline(cin,
          hello); // internal function for the whole input of string/sentence
  cout << hello;  // cout of the input. just get the gitcommit
  return 0;
}